<desc class="omni-chart__tooltip-description">@(Enabled ? Description : "Les infobulles sont désactivées.")</desc>

@code {
    [Parameter] public bool Enabled { get; set; } = true;
    [Parameter] public string Description { get; set; } = "Les valeurs détaillées sont disponibles au focus ou au survol des séries.";
}
