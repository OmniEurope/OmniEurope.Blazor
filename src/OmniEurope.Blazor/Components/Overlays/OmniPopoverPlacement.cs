namespace OmniEurope.Blazor.Components;

/// <summary>Where the panel of an <see cref="OmniPopover"/> opens, relative to its trigger.</summary>
public enum OmniPopoverPlacement
{
    /// <summary>Below the trigger, its end edge aligned on the trigger's end edge.</summary>
    BottomEnd,

    /// <summary>Below the trigger, its start edge aligned on the trigger's start edge.</summary>
    BottomStart
}
